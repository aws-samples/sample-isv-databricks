# Floors match the versions this sample was validated against, not arbitrary minimums.
# mcp in particular must be able to negotiate the MCP protocol version the gateway
# declares (see MCP_PROTOCOL_VERSION in gateway_setup.py); older clients cap out below it
# and fail the initialize handshake.
strands-agents~=1.53
bedrock-agentcore~=0.1.7
# Provides the `agentcore` CLI the Runtime steps in the README depend on; the
# bedrock-agentcore package alone does not install that entry point.
bedrock-agentcore-starter-toolkit~=0.1
mcp~=1.29
boto3~=1.43
requests~=2.34
pyyaml~=6.0
python-dotenv~=1.2
